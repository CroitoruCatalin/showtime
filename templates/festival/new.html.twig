{% extends 'layouts/admin-base.html.twig' %}

{% block title %}New Festival{% endblock %}

{% block content %}
    <h1>Create new Festival</h1>

    {{ include('festival/_form.html.twig', {
        'button_label' : 'Create',
        'form_title': 'Add a festival'
    }) }}

    <a href="{{ path('admin_festival_index') }}">back to list</a>
{% endblock %}
